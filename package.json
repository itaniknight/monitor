{
    "scripts": {
        "pug": "pug -w dev/pug -o app -P",
        "sass": "node-sass -w dev/sass -o app/css -r --output-style compressed --indent-type space --indent-width 4 --error-bell",
        "sync": "browser-sync start -s app -f app --reload-delay 250 --reload-debounce 250 --no-open --no-online --no-ghost-mode"
    }
}
